apiVersion: zookeeper.kubedoop.dev/v1alpha1
kind: ZookeeperCluster
metadata:
  name: test-zk
spec:
  clusterConfig:
    listenerClass: cluster-internal
  server:
    roleGroups:
      default:
        replicas: 1
